#pragma once

enum ReqState {
	RS_IDLE,
	RS_START_LINE_READ,
	RS_START_LINE_DONE,
	RS_HEADERS_READ,
	RS_HEADERS_DONE,
	RS_READING_BODY,
	RS_NO_BODY,
	RS_BODY_DONE,
	RS_ROUTING_DONE,
	RS_READY,
	RS_CLOSE,
};

enum	e_type {
	REDIRECTION,
	DIRECTORY,
	CGI,
	REG_FILE
};
